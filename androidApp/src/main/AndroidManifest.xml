<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AgriloApplication"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:label="Agrilo"
        android:theme="@style/Theme.Agrilo">

        <!--
          allowBackup="false" : évite qu'un token de session ne se
          retrouve dans une sauvegarde cloud non chiffrée du téléphone.
          networkSecurityConfig : interdit tout trafic HTTP en clair,
          uniquement HTTPS vers l'API.
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>